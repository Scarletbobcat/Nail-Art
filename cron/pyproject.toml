[project]
name = "cron"
version = "0.1.0"
description = "Maintenance jobs for the Nail Art appointment book"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "psycopg[binary]>=3.2.0",
    "python-dotenv>=1.0.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.0",
]
